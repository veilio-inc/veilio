{
  "name": "@veilio-inc/mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the anonymizer to coding agents, so a model can work on masked code without the real identifiers ever entering its context.",
  "type": "module",
  "bin": {
    "veilio-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "anonymizer",
    "llm",
    "privacy",
    "secret-scanning",
    "veilio"
  ],
  "license": "SEE LICENSE IN LICENSE",
  "//private": "Still unpublished. `license` is now settled — the Veilio Community License 1.0, the same terms the engine this wraps ships under, and the only terms compatible with the `npx @veilio-inc/cli` usage packages/cli/action.yml advertises. One decision remains: flipping `private` arms `changeset publish` in CI, which publishes on the next Version Packages merge rather than when somebody decides to release. Note also that docs/RELEASE_RUNBOOK.md states nothing publishes from this repo — publishing these two from here amends that policy, or they move to the public repo first. See ROADMAP.md E5/E6.",
  "private": true,
  "dependencies": {
    "@veilio-inc/engine": "^1.3.0",
    "@veilio-inc/cli": "^0.1.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/veilio-inc/veilio-cloud.git",
    "directory": "packages/mcp"
  },
  "bugs": {
    "url": "https://github.com/veilio-inc/veilio-cloud/issues"
  },
  "homepage": "https://github.com/veilio-inc/veilio-cloud/tree/main/packages/mcp#readme",
  "publishConfig": {
    "access": "public"
  }
}
