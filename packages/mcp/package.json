{
  "name": "@veilio-inc/mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the anonymizer to coding agents, so a model can work on masked code without the real identifiers ever entering its context.",
  "type": "module",
  "bin": {
    "veilio-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=24"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "anonymizer",
    "llm",
    "privacy",
    "secret-scanning",
    "veilio"
  ],
  "license": "UNLICENSED",
  "//private": "Still unpublished, and `private` is the only switch left — the metadata above is publish-ready. Two things must be decided first, neither of them mechanical: (1) `license` still reads UNLICENSED, which contradicts publishing at all — the public artifacts ship under the Veilio Community License 1.0 (see docs/RELEASE_RUNBOOK.md) and this needs to say so before a stranger installs it; (2) flipping this arms `changeset publish` in CI, which publishes on the next Version Packages merge rather than when somebody decides to release. See ROADMAP.md E5/E6.",
  "private": true,
  "dependencies": {
    "@veilio-inc/engine": "^1.2.0",
    "@veilio-inc/cli": "^0.1.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/veilio-inc/veilio-cloud.git",
    "directory": "packages/mcp"
  },
  "bugs": {
    "url": "https://github.com/veilio-inc/veilio-cloud/issues"
  },
  "homepage": "https://github.com/veilio-inc/veilio-cloud/tree/main/packages/mcp#readme",
  "publishConfig": {
    "access": "public"
  }
}
