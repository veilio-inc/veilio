{
  "name": "@veilio-inc/mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the anonymizer to coding agents, so a model can work on masked code without the real identifiers ever entering its context.",
  "type": "module",
  "bin": {
    "veilio-mcp": "dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "anonymizer",
    "llm",
    "privacy",
    "secret-scanning",
    "veilio"
  ],
  "license": "SEE LICENSE IN LICENSE",
  "//private": "Still unpublished, and this is the only thing left between here and the registry. Flipping `private` arms `changeset publish` on the next Version Packages merge. Terms are settled: the Veilio Community License 1.0, the same terms packages/engine ships under, and the only terms compatible with the `npx @veilio-inc/cli` usage packages/cli/action.yml advertises. Publishing from THIS repo is the point of the move out of the closed Cloud repository — see docs/PUBLISHING.md.",
  "private": true,
  "dependencies": {
    "@veilio-inc/engine": "^1.4.0",
    "@veilio-inc/cli": "^0.1.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run typecheck && npm run build && npm run test",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/veilio-inc/veilio.git",
    "directory": "packages/mcp"
  },
  "bugs": {
    "url": "https://github.com/veilio-inc/veilio/issues"
  },
  "homepage": "https://github.com/veilio-inc/veilio/tree/main/packages/mcp#readme",
  "publishConfig": {
    "access": "public"
  }
}
